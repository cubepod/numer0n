# ヌメロン（Numer0n）Webゲーム実装計画

この計画は、ブラウザ上で動作する対戦型ヌメロン（Numer0n）ゲームの開発を目的としています。単なる数字当てゲームではなく、プレイヤーとCPUが互いに推理し合う本格的な対戦モードを、洗練されたサイバーパンク風のプレミアムデザインで実装します。

## 改善点と厳格なアドバイス
ユーザーの要件である「3桁の数字をランダムに生成する」をそのまま単純に実装するだけでは、プレイヤーが一方的に当てるだけの「数当てゲーム」になり、ゲームとしての魅力が極めて低くなります。
ヌメロンの真の面白さは、「相手の推理速度との競争」および「論理的な推論プロセス」にあります。
そのため、以下の機能を標準で盛り込む必要があります。
1. **CPU対戦モード**: プレイヤーとCPUが交互にコールし合い、先に相手の数字を当てた方が勝利する。
2. **論理的推論CPU AI**: CPUは完全なランダムでコールするのではなく、これまでのコール履歴と結果（EAT/BITE）から矛盾しない数字の候補（全720通りからフィルタリング）を割り出し、論理的に絞り込んでコールします。これにより、プレイヤーにとって「手強いライバル」となります。
3. **誤入力防止カスタムテンキー**: 3桁の重複しない数字を入力する際、すでに選択した数字ボタンを無効化することで、ユーザーの重複入力ミスを完全に排除します。

---

## ユーザー確認事項・選択肢

> [!IMPORTANT]
> ゲーム体験と実装範囲を確定させるため、以下の2点についてご意見をお聞かせください。
> 1. **本家アイテム機能の追加**: 本家ヌメロンにある「HIGH-LOW（相手の数字がそれぞれ高いか低いか知る）」「TARGET（相手の数字の特定の桁を特定する）」などのアイテム機能を実装しますか？（実装する場合、戦略性が増しますがUIがより複雑になります。まずはアイテムなしのスタンダードな対戦から実装することを推奨します）
> 2. **デザインの方向性**: 現在は「ネオンサイバーパンク（暗めの背景に蛍光グリーンのグリッドと光彩効果）」を予定していますが、「シンプルモダン（洗練された白・グレー基調のミニマルデザイン）」への変更を希望されますか？

---

## 提案する変更点

SPA（シングルページアプリケーション）構成として、以下の3ファイルを作成します。外部ライブラリは使用せず、純粋なHTML/CSS/JavaScriptのみで構築することで、高速な動作と高いカスタマイズ性を両立します。

### フロントエンド・ゲームロジック

#### [NEW] [index.html](file:///d:/gemini_works/numeron/index.html)
- アプリケーションの構造を定義します。
- ゲーム開始画面（自分の数字の決定、CPU戦の開始）、対戦中の画面（プレイヤーとCPUのボード、履歴、入力用テンキー）、結果画面（勝利・敗北のダイナミックな演出）を含みます。
- Google Fonts から「Orbitron」および「Inter」フォントを読み込み、近未来的な雰囲気を演出します。
- FontAwesome（CDN）を利用して、直感的なアイコンを表示します。

#### [NEW] [style.css](file:///d:/gemini_works/numeron/style.css)
- プレミアムなビジュアルスタイルを定義します。
- **デザインシステム**:
  - バックグラウンド: 深いダークブルー/グレーのグラデーション (`#0a0b10` から `#121520`)
  - アクセントカラー: ネオングリーン (`#00ff66`)、ネオンブルー (`#00f0ff`)、ネオンレッド (`#ff0055`)
  - ガラスモフィズム: `backdrop-filter: blur(12px)` を使用した透明感のあるパネル
  - グリッドシステムとネオンシャドウによる奥行き感の表現
- 状態遷移やアタック時のアニメーション（数字が回転するスロット風アニメーション、EAT/BITEのバッジが光るエフェクト）を実装します。

#### [NEW] [script.js](file:///d:/gemini_works/numeron/script.js)
- ゲームロジックおよびCPUの推論AIを実装します。
- **主要なロジック**:
  - `generateRandomNumber()`: 重複のない3桁の数字をランダムに生成するロジック（CPUの持ち数、およびプレイヤーのランダム生成用）。
  - `calculateEatBite(target, guess)`: 二つの数字を比較し、EAT（位置も数字も一致）とBITE（数字のみ一致）の数を計算する。
  - **CPU推論AI (`CpuPlayer` クラス)**:
    - 0-9の重複しない3桁の組み合わせ（合計720通り）を初期候補リストとして保持。
    - プレイヤーがアタック結果を返すたびに、その推測値とEAT/BITE結果に矛盾する（同じEAT/BITE結果を返さない）候補をリストから削除。
    - 残った候補リストから、次の推測値を論理的に選択（難易度に応じてランダム性や推論の精度を調整可能にする）。
  - **Web Audio APIによるサウンドエフェクト**:
    - ボタンクリック、決定、EAT/BITE判定、勝利、敗北の各イベントに対応する、電子的な効果音をシンセサイズして再生します（ミュート切り替えボタン付き）。

---

## 検証計画

### 自動テスト / ユニットテスト
- `calculateEatBite` 関数の動作検証（エッジケース含む）。
  - 例：`123` と `123` -> 3 EAT
  - 例：`123` と `321` -> 1 EAT, 2 BITE
  - 例：`123` と `456` -> 0 EAT, 0 BITE
- CPU AIの候補絞り込みロジックのシミュレーションテスト（CPUが有限手数（平均5.5手前後）で必ずプレイヤーの数字を当てられるかの検証スクリプトを実行）。

### 手動検証
1. ブラウザで `index.html` を起動し、以下の手順でプレイテストを行う。
   - プレイヤーの初期数字設定画面で、重複する数字の入力が防がれているか。
   - プレイヤーのアタック時に、正しくEAT/BITEが判定され、履歴に追加されるか。
   - CPUがターンごとに論理的なアタックを行い、履歴が表示されるか。
   - 3 EATを達成した際、またはCPUに達成された際に、勝敗の演出が正しく表示されるか。
   - 音声トグルが機能し、効果音のON/OFFが切り替えられるか。
2. モバイル端末の画面解像度を開発者ツールでシミュレートし、UIが崩れずに操作可能であることを確認する。
